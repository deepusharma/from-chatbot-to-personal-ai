# Session — 2026-05-19 (Session 02)

**Session name:** FromChatbotToPersonalAI - 02
**Started:** 2026-05-19
**Closed:** —

---

## What Was Done

| Area | Detail |
|---|---|
| Folder structure | Created `~/Documents/experiments/from-chatbot-to-personal-ai/` with `vault/` and `tools/` subdirectories. Moved test vault from `~/Documents/personal-ai-test-vault/`. Updated all 11 articles and session notes with new path. |
| OpenClaw install | Confirmed correct install command: `npm install -g openclaw@latest`. Fixed article: Node minimum is v22.19.0, not v18. Documented pnpm reinstall requirement with nvm. |
| OpenClaw config | Configured OpenRouter (rate limits + content filter issues), then switched to Ollama. Set gemma4:latest as primary, llama3.2:3b as fallback. Gateway auth token set. |
| Vault connection | Discovered Obsidian is NOT required. Vault connected as MCP filesystem server via `@modelcontextprotocol/server-filesystem`. |
| Article updates | Setup-A rewritten: installation section, model config section, Node/pnpm prerequisites. All reflect real tested flow. |
| Commits | Two commits pushed: vault path updates + CLAUDE.md (45bfacd), Setup-A install/model fix (5008e07). |

---

## Key Decisions

- Obsidian + ObsidianClaw plugin is NOT required. OpenClaw reads vault via MCP filesystem server.
- OpenRouter free tier is unreliable for testing: rate limits, content filters, stuck sessions.
- Ollama is the right choice for test sessions: no rate limits, no content filters, local.
- gemma4:latest (9.6GB, already downloaded) is the test model. llama3.2:3b as fallback.
- Workspace stays at `~/.openclaw/workspace`. Content sources added as MCP servers.

---

## Still Open

| Item | Note |
|---|---|
| First successful test query | Gateway was loading gemma4 (9.6GB) when session closed — not yet confirmed working |
| Test query outputs | All three queries still [FILL] in Setup-A |
| Resource usage table | Requires successful run |
| Setup time table | Requires successful run |
| WhatsApp setup | Low priority — MCP path is more useful and Obsidian-independent |
| Article framing update | Setup-A still frames vault connection as Obsidian-dependent — needs update to reflect MCP path |
